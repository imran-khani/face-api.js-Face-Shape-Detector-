// This script initializes face-api.js when needed
// It will be loaded after the main content is rendered
(function() {
  // This will only run when the script is loaded
  console.log('Face API initialization script loaded');
  
  // You can add code here to preload models or initialize face-api
  // when the user interacts with the face detection feature
})();